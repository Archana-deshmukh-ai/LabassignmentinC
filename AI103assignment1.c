#include<stdio.h>

int main() {
  
    // Input values
    float P = 200, R = 100, T = 100;

    // Calculate simple interest
    float SI = (P * T * R) / 100;

    // Print Simple Interest
    printf("Simple Interest = %f\n", SI);

    return 0;
}

    
    
